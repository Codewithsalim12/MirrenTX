Use existing code patterns, centralized and scalable logic, performant and optimized code to fix.
Use industry standard Best practices for code patterns, reuse existing code patterns (if they already use industry standard best practices), write centralized, performant, optimized and scalable code.
Use proper types fixes, scan code base for relevant files and get context from them and then fix, dont remove any logic (unless you are confident that it is stale), but make it work.
Use Context 7 for docs.
Make changes only when you are more than 95% confident, Ask me questions that will help you become more than 95% confident.
Please ensure all changes maintain type safety (TypeScript), follow React hooks best practices, and result in clean, maintainable code.